server:
  port: 8888
spring:
  application:
    name: config-server-service
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ctorrez04/demo-config-server-repo
      #discovery:
        #enabled: true
      fail-fast: true